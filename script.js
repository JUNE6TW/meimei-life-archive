const sections=[...document.querySelectorAll('.page')];const dots=document.getElementById('dots');sections.forEach((section,index)=>{const a=document.createElement('a');a.href=`#${section.id}`;a.setAttribute('aria-label',`Go to page ${index+1}`);dots.appendChild(a)});const dotLinks=[...dots.querySelectorAll('a')];const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){const i=sections.indexOf(entry.target);dotLinks.forEach(d=>d.classList.remove('active'));if(dotLinks[i])dotLinks[i].classList.add('active')}})},{threshold:.45});sections.forEach(section=>observer.observe(section));document.querySelector('.scroll-top').addEventListener('click',()=>{window.scrollTo({top:0,behavior:'smooth'})});